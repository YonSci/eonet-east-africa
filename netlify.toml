[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION       = "20"
  VITE_EONET_DIRECT  = "true"

# SPA fallback: all routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
